# Rust GRPC Proxy

Максимально быстрый GRPC прокси для мониторинга Create транзакций Pump.fun.

## Особенности

- ⚡ **Максимальная скорость** - прямой TCP socket (без HTTP overhead)
- 🚀 **Прямое GRPC подключение** - детект в 0/+1 блоке
- 📦 **Бинарный протокол** - bincode сериализация (быстрее JSON)
- 🔄 Автоматическое переподключение при обрыве соединения

## Использование

1. Запустите GRPC прокси:
```bash
./rust-grpc-proxy.exe
```

2. Прокси будет слушать TCP socket на `localhost:8725`

3. Снайпер автоматически подключится к этому socket

## Сборка

### Локально (Windows)
```bash
cargo build --release --target x86_64-pc-windows-gnu
```

### GitHub Actions
Workflow автоматически собирает `.exe` при push в main/master.

## Производительность

- Задержка GRPC → TCP socket: **<1ms**
- Парсинг bincode: **<1ms**
- **Итого: ~2ms** задержка (вместо ~60-120ms с HTTP/SSE)

